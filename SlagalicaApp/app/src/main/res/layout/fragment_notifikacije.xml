<?xml version="1.0" encoding="utf-8"?>
<!-- Fragment notifikacija - RecyclerView + filter (pročitane/nepročitane) -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <!-- Filter chipovi (Sve / Pročitane / Nepročitane) -->
    <com.google.android.material.chip.ChipGroup
        android:id="@+id/chipGroupFilter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="8dp"
        android:background="@color/surface"
        app:singleSelection="true"
        app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior">

        <com.google.android.material.chip.Chip
            android:id="@+id/chipSve"
            style="@style/Widget.MaterialComponents.Chip.Filter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_sve"
            android:checked="true"/>

        <com.google.android.material.chip.Chip
            android:id="@+id/chipNeprocitane"
            style="@style/Widget.MaterialComponents.Chip.Filter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_neprocitane"/>

        <com.google.android.material.chip.Chip
            android:id="@+id/chipProcitane"
            style="@style/Widget.MaterialComponents.Chip.Filter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/btn_procitane"/>

    </com.google.android.material.chip.ChipGroup>

    <!-- RecyclerView za notifikacije - kao na vjezbi GUI II -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvNotifikacije"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="60dp"
        android:clipToPadding="false"
        android:paddingBottom="16dp"
        tools:listitem="@layout/item_notifikacija"
        xmlns:tools="http://schemas.android.com/tools"/>

    <!-- Prazna lista poruka -->
    <LinearLayout
        android:id="@+id/llPraznoStanje"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔔"
            android:textSize="64sp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/lbl_prazna_lista"
            android:textColor="@color/text_secondary"
            android:textSize="18sp"
            android:gravity="center"/>

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
